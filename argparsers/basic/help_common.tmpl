{{define "header"}}Basic Parser

Queries are designed to process, filter, and summarize data effectively.
Specifically, queries allow you to:
- Filter out irrelevant data
- Select specific components of the data to view
- Group, summarize, and calculate metrics on the data

You can construct queries progressively. Here are examples of how to build them:{{end}}

{{define "sortIntro"}}	Explanation: This sorts the resulting data based on the specified fields.{{end}}

{{define "comboIntro"}}	Operations can be chained together sequentially to achieve complex data processing:{{end}}

{{define "notesIntro"}}- String literals consisting of a single word should begin with a dot (`.`).
- Complex boolean filters can be composed using `and`, `or`, and `not`.
- You can group conditions using parentheses `(` and `)`.
  **Note on Shell Escaping:** Since the shell treats parentheses specially, you must escape or quote them when invoking the CLI, e.g., `\( c.age gt .10 or c.age lt .20 \)` or `"\(" c.age gt .10 or c.age lt .20 "\)"`.
- Valid comparison operators are: `eq`, `contains`, `icontains`, `gt`, `gte`, `lt`, `lte`.{{end}}

{{define "footer"}}- All functions must be preceded by `f.`.
- Extension PRs are welcome and encouraged!{{end}}